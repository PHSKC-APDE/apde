#' Move addresses from the informatica output to ref.stage_address_clean
#' 
#' 


# compare whether there are rows in ref.informatica_output that are not in ref.stage_address_clean

# move rows that don't have `#` in geo_add2_raw into stage
# also create geohash

#repeat with address where `#` might be problem.

